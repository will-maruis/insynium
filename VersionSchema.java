package plm.model;

import javax.persistence.Entity;

@Entity
public class VersionSchema {
	// ??  existe pas d'attributs
	// quelles sont les attributs sur lesquelles la méthode va s'implmenter. 
	// on pourrait définir une collection d'objets version :  List<Version>
	
	public String getNextVersionLabel(String currentVersionLabel) {
		//
		// Implementation and returned value are not relevant for this exercise
		// ietération : interface iteratorList, qui va nous permettte grace à ses méthodes  
		return null;
	}
}
